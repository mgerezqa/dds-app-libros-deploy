# Use postgres/example user/password credentials
version: '3.1'

services:

  dbservice:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: dblibros
    volumes:
    - ./posgresdb:/var/lib/postgresql/data

  app:
    image: ddsdeploy
    restart: always
    ports:
      - 8070:8070
    environment:
       hibernate__hbm2ddl__auto: create
       javax__persistence__jdbc__driver: org.postgresql.Driver
       javax__persistence__jdbc__url: jdbc:postgresql://dbservice/dblibros
       javax__persistence__jdbc__password: admin
       javax__persistence__jdbc__user: admin
    depends_on:
     - dbservice
